
!!! CHANGE IMAGE

Comments:
A document type declaration, or DOCTYPE, is an instruction that associates a particular document (for example, a webpage)
with a document type definition (DTD) (for example, the formal definition of a particular version of HTML).